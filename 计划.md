# Qoder - MP3 格式转换工具 实现计划

## 背景

用户需要一个桌面工具：选择/拖放音乐或视频文件，转换为 MP3 格式，最终打包为 Windows 可执行文件 (.exe)。这是 `D:\codes\qoder_data\media_to_mp3\media_to_mp3` 下的项目。

---

## 技术选型

| 层面 | 选择 | 理由 |
|------|------|------|
| 语言 | **Python 3.12** | 开发速度快，生态成熟，打包工具完善 |
| GUI | **PyQt5** | 专业桌面 UI，拖放支持，信号/槽机制与异步进程天然配合 |
| 转换引擎 | **FFmpeg (gyan.dev 精简版)** | 行业标准，支持所有音视频格式，可获取进度信息 |
| 元数据 | **mutagen** | 可靠的 ID3 标签读写，支持封面图片 |
| 打包 | **PyInstaller** | 最成熟的 Python→exe 工具，单文件夹模式 |

---

## 目录结构

```
D:\codes\qoder_data\media_to_mp3\
├── src\
│   ├── main.py                    # 入口: QApplication, 高DPI, MainWindow
│   ├── ui\
│   │   ├── main_window.py         # 顶层窗口聚合所有组件
│   │   ├── file_drop_widget.py    # 拖放区域 + 浏览按钮 + 空状态
│   │   ├── queue_widget.py        # 文件队列表格 + 进度条 + 右键菜单
│   │   ├── settings_panel.py      # 比特率/输出目录/选项
│   │   └── about_dialog.py        # 关于对话框
│   ├── core\
│   │   ├── converter.py           # 编排: 遍历队列, 调用 FFmpeg, 处理结果
│   │   ├── queue_manager.py       # 队列增删改查/排序/去重/持久化
│   │   └── ffmpeg_wrapper.py      # QProcess 子进程, 进度解析, 错误映射
│   └── utils\
│       ├── resource_manager.py    # FFmpeg 路径解析(开发/打包模式)
│       ├── metadata_handler.py    # mutagen ID3 标签读写
│       ├── file_utils.py          # 格式嗅探/路径清理/大小格式化
│       └── format_registry.py     # 支持格式字典(含类别/MIME)
├── tools\ffmpeg\                  # FFmpeg 精简版二进制
│   └── ffmpeg.exe (+ DLL)
├── packaging\
│   └── build.spec                 # PyInstaller 打包配置
├── tests\                         # pytest 单元测试
└── requirements.txt               # PyQt5, mutagen, pyinstaller
```

---

## 核心架构

### 数据流

```
用户拖放文件 → GUI 信号 → queue_manager.add_files()
用户点击转换 → converter.start() → 遍历队列
    ↓
FFmpegWrapper (QProcess 异步执行 FFmpeg)
    ↓ 解析 stderr 获取 out_time_us / speed
    ↓ 发射 progress_updated 信号
    ↓
QueueWidget 更新进度条
完成后 → MetadataHandler 写 ID3 标签 → 标记完成
全部完成 → 弹出汇总对话框
```

### 线程模型

- **无需额外 QThread**：QProcess 自身异步，通过 Qt 事件循环触发 `readyRead*` 信号
- FFmpeg 在独立 OS 进程运行，不阻塞 GUI
- 队列逐个处理，暂不支持多文件并行转换

### FFmpeg 关键命令

```
# 视频→MP3 (提取音轨)
ffmpeg -i "input.mp4" -vn -codec:a libmp3lame -b:a 192k -id3v2_version 3 -progress pipe:1 -y "output.mp3"

# 音频→MP3 (不传 -vn)
ffmpeg -i "input.wav" -codec:a libmp3lame -b:a 192k -id3v2_version 3 -progress pipe:1 -y "output.mp3"
```

进度通过正则 `out_time_us=(\d+)` 从 stdout 解析。

---

## 实现阶段

### 阶段 1：骨架 + 基础设施
- 建目录结构，写 `requirements.txt`，下载 FFmpeg 二进制到 `tools/ffmpeg/`
- 实现：`format_registry.py` (支持格式字典) + `file_utils.py` (格式检测、路径处理、输出路径生成)
- 测试：`test_format_registry.py`, `test_file_utils.py`

### 阶段 2：核心转换引擎
- 实现：`resource_manager.py` → `ffmpeg_wrapper.py` (QProcess+进度解析) → `metadata_handler.py` (mutagen 标签) → `queue_manager.py` → `converter.py` (编排器)
- 测试：各模块单元测试 + 样本文件手动集成测试

### 阶段 3：GUI 界面
- 实现：`file_drop_widget.py` (拖放区+空状态) → `settings_panel.py` (比特率+目录+选项) → `queue_widget.py` (进度表+右键菜单) → `main_window.py` (组装全界面) → `main.py` (入口)
- 连接阶段 2 信号到界面槽函数

### 阶段 4：集成与打磨
- 批处理流程 (队列进度/文件间过渡)
- 错误处理: 文件被占用/磁盘满/解码失败/格式不支持 等
- 边缘情况: Unicode文件名/长路径/超大文件/空队列
- 手动 QA 所有支持格式

### 阶段 5：打包成 exe
- 编写 `build.spec`，排除 QtWebEngine/QtSql/numpy 等不需要的模块
- 构建命令: `pyinstaller packaging\build.spec`
- 在无 Python 的测试机上验证 exe 运行
- 预估大小: ~50-60MB (含 Python 运行时 + Qt DLL + FFmpeg)

---

## 错误处理

| 场景 | 检测方式 | UI 表现 |
|------|---------|---------|
| FFmpeg 未找到 | QProcess 启动失败 | 对话框提示重装 |
| 格式不支持 | `file_utils` 过滤 | 添加时静默跳过 |
| 文件被占用 | QProcess 退出码非0+stderr | 该项标记 "文件被占用" |
| 磁盘满 | stderr 含 "No space left" | 暂停队列+对话框提示 |
| 源文件损坏 | stderr 含 "Invalid data" | 该项标记 "源文件损坏" |
| 输出冲突 | `os.path.exists()` | 自动追加数字后缀 |

---

## 验证方式

1. **单元测试**: 各模块 pytest 测试 (mock QProcess/mutagen)
2. **集成测试**: 用真实的 WAV/MP4/FLAC 样本文件转换，验证输出 MP3 存在、非空、时长匹配
3. **手动测试**:
   - 拖放单文件/多文件/文件夹
   - 含空格和中文的路径
   - 取消进行中的转换
   - 在无 Python 环境的 Windows 机器上运行最终 exe
4. **性能验证**: 测试大文件 (100MB+) 转换速度，确保 UI 不卡顿
